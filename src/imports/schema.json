{
  "$schema": "http://json-schema.org/schema",
  "id": "ImportsSchema",
  "title": "Creates import statement snippets.",
  "type": "object",
  "properties": {
    "name": {
      "description": "The path and name of the component/service",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "name"
  ]
}
